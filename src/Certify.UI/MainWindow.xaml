<Controls:MetroWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        xmlns:local="clr-namespace:Certify.UI"
        xmlns:Controls1="clr-namespace:Certify.UI.Controls"
        xmlns:fa="http://schemas.fontawesome.io/icons/" x:Class="Certify.UI.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        GlowBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        mc:Ignorable="d"
        Title="Certify SSL Certificate Management" WindowTransitionsEnabled="False" TitleCaps="False" Height="668" MinWidth="680" MinHeight="400" Width="1120" WindowStyle="ToolWindow" Loaded="MetroWindow_Loaded" ContentRendered="MetroWindow_ContentRendered">
    <Controls:MetroWindow.Resources>

        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Dragablz;component/Themes/mahapps.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Controls:MetroWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="49"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <DockPanel>
            <materialDesign:ColorZone Grid.Row="0" Height="49" materialDesign:ShadowAssist.ShadowDepth="Depth2" Mode="PrimaryDark" DockPanel.Dock="Top">
                <DockPanel>
                        <Button Height="45" Margin="0" Click="Button_NewCertificate" Style="{StaticResource MaterialDesignFlatButton}">
                            <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Globe"  Height="28" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource ButtonIcon}" />
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Foreground="Black"><Run Text="New Certificate" /></TextBlock>
                            </StackPanel>
                        </Button>
                        <Button Height="45" Margin="0" Click="Button_RenewAll" Style="{StaticResource MaterialDesignFlatButton}">
                            <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Refresh"  Height="28" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource ButtonIcon}" />
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Foreground="Black"><Run Text="Renew All" /></TextBlock>
                            </StackPanel>
                        </Button>
                        <Button Height="45" Margin="8,0,0,0" Click="Button_ScheduledTaskConfig" Style="{StaticResource MaterialDesignFlatButton}">
                            <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Calendar" Height="28" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource ButtonIcon}" />
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Foreground="Black"><Run Text="Configure Auto Renew" /></TextBlock>
                            </StackPanel>
                        </Button>
                        <StackPanel Orientation="Horizontal" Visibility="{Binding IsUpdateAvailable, Converter={StaticResource ResourceKey=BoolToVisConverter}}">
                            <Button x:Name="ButtonUpdateAvailable" Height="45" Margin="0" Background="GreenYellow" Click="ButtonUpdateAvailable_Click">
                                <StackPanel Orientation="Horizontal">
                                <fa:ImageAwesome Icon="Download"  Height="28" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource ButtonIcon}" />
                                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center"><Run Text="Update Available" /></TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>
        </DockPanel>

        <dragablz:TabablzControl x:Name="MainTabControl" Grid.Row="2" Margin="0" BorderThickness="0"
                                 SelectedIndex="{Binding MainUITabIndex}" HorizontalContentAlignment="Left" VerticalContentAlignment="Stretch" Height="auto">

            <TabItem Header="Managed Sites" IsSelected="True">
                <Controls1:ManagedSites Width="Auto" Height="Auto" />
            </TabItem>
            <!--<TabItem Header="Vault" VerticalContentAlignment="Stretch" HorizontalAlignment="Left" HorizontalContentAlignment="Left">
                <Controls1:VaultExplorer Width="Auto" Height="Auto" />
            </TabItem>-->
            <TabItem Header="In Progress" VerticalContentAlignment="Stretch" HorizontalAlignment="Left" HorizontalContentAlignment="Left">
                <Controls1:ProgressMonitor Width="Auto" Height="Auto" />
            </TabItem>
            <TabItem Header="Settings" VerticalContentAlignment="Stretch" HorizontalAlignment="Left" HorizontalContentAlignment="Left">
                <Controls1:Settings  Width="Auto" Height="Auto" />
            </TabItem>
            <TabItem Header="About" VerticalContentAlignment="Stretch" HorizontalAlignment="Left" HorizontalContentAlignment="Left">
                <Controls1:AboutControl Width="Auto" Height="Auto" />
            </TabItem>
        </dragablz:TabablzControl>
    </Grid>
</Controls:MetroWindow>