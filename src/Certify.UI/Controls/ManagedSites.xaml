<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:Certify.UI.Controls"
             xmlns:Models="clr-namespace:Certify.UI.Models" xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="Certify.UI.Controls.ManagedSites"
             mc:Ignorable="d"
             d:DesignHeight="300" Width="809.302">
    <DockPanel>
        <ListView Height="261" Margin="0,0,0,19" VerticalAlignment="Bottom" ItemsSource="{Binding AppViewModel.ManagedSites}" Width="261">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="Name: " />
                        <TextBlock Text="{Binding SiteName}" FontWeight="Bold" />
                        <TextBlock Text=", " />
                        <TextBlock Text="Id: " />
                        <TextBlock Text="{Binding SiteId}" FontWeight="Bold" />
                        <TextBlock Text=" (" />
                        <TextBlock Text="{Binding SiteType}" TextDecorations="Underline" Foreground="Blue" Cursor="Hand" />
                        <TextBlock Text=")" />
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.DataContext>
                <Models:AppModel />
            </ListView.DataContext>
        </ListView>
        <StackPanel Height="300" VerticalAlignment="Top" Width="516" />
    </DockPanel>
</UserControl>